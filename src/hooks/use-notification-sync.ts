/**
 * Sync notifications — intégré dans FarmStoreProvider (FarmNotificationEffects).
 * Ce hook documente le point d'entrée ; la logique vit dans farm-store.tsx.
 */
export { useNotificationsStore } from "@/contexts/farm-store";
