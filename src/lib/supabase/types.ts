/**
 * Réexport des types cibles Postgres — source : `src/types/supabase-schema.ts`.
 * Remplacé plus tard par les types générés (`supabase gen types typescript`).
 */

export type {
  CategorieDepenseRow,
  MethodePaiementRow,
  FarmProfileRow,
  FarmPreferencesRow,
  FarmSeuilsRow,
  FarmNotificationRow,
  FarmReportRow,
} from "@/types/supabase-schema";
